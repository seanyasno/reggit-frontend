import {makeStyles} from '@material-ui/core';

const useCardStyle = makeStyles({
    style: {
        margin: 'auto',
        padding: '0.8em 1em',
        textAlign: 'start',
        borderRadius: '1em',
        maxWidth: '30%',
        minWidth: '400px',
    }
});

export default useCardStyle;
